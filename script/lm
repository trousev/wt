#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

if [ ! -d "$SCRIPT_DIR/.venv" ]; then
    echo "==> .venv not found, running update..."
    "$SCRIPT_DIR/script/update"
fi

exec "$SCRIPT_DIR/.venv/bin/python" "$SCRIPT_DIR/bin/lm" "$@"